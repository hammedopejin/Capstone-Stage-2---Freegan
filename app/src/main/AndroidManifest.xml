<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.planetpeopleplatform.freegan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity" />
        <activity android:name=".fragment.ImagePagerActivity" />
        <activity android:name=".activity.RegisterActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LoginActivity" />
        <activity
            android:name=".activity.SettingsActivity"
            android:label="@string/title_activity_settings" />

        <provider
            android:authorities="com.planetpeopleplatform.freegan"
            android:name="com.planetpeopleplatform.freegan.data.FreeganProvider"
            android:exported="false"
            android:syncable="true"/>

    </application>

</manifest>